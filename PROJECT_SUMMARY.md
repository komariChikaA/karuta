# PROJECT SUMMARY

## 项目现状

项目已具备可实际开局的完整流程：

- 数据集维护（GUI + CSV）
- 开局前选卡
- 回合判定与休息阶段
- 管理员面板实时干预

## 已实现能力

- CSV 读写（支持引号、UTF-8 BOM）。
- 多曲目作品模型。
- `flac -> mp3` 自动转码导入。
- 休息音乐池（未参赛卡牌优先）。
- `PASS/SKIP` 失败规则。
- 对局 UI 与后台面板联动。

## 当前重点

- 文档体系已重写并与代码行为对齐。
- 配置、数据和媒体目录规范已统一。
- 维护侧应优先保证：CSV 兼容、素材路径一致、按钮流程一致。
